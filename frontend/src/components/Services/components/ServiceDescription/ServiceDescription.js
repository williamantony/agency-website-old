import React from 'react';
import './ServiceDescription.css';

const ServiceDescription = (props) => (
  <div className="ServiceDescription">{ props.children }</div>
);

export default ServiceDescription;
